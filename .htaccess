# Instrucciones al servidor Apache para indicarle que las peticiones las redireccione a la carpeta public
<IfModule mod_rewrite.c >
    RewriteEngine on
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>